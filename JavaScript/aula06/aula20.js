// Press "F8" to run
let num = [5, 8, 2]  // Definição de array/vetor/variável composta
console.log(num)
console.log(`O primeiro valor do vetor é ${num[0]}`)

num[3] = 4  // Adicionando um valor no indíce 3, mesmo que esse indíce ainda não exista
num.push(7)  // Criando mais uma posição para o array e adicinando o número 7
console.log(num)
