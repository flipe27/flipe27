// Press "F8" to run
let valores = [5, 8, 2, 4, 3]
console.log(valores)

for (let pos in valores) {  // Outra maneira de percorrer o vetor
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}
