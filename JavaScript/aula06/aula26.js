// Press "F8" to run
function soma(n1 = 0, n2 = 0) {  // Predefinição dos valores dos parâmetros
    return n1 + n2
}

console.log(soma(2))  // Passando um parâmetro para uma função que precisa de dois parâmetros
