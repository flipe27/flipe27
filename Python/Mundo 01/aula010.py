import random
import emoji

num = random.randint(1, 10)  # Gera um número inteiro aleatório entre os parâmetros informados
print(num)
print(emoji.emojize('Já sorteei o número, é nois :sunglasses:'))  # Mostra o emoji passado como parâmetro
